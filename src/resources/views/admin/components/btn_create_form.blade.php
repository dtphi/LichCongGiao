@if(!empty($id))
    <div class="d-flex justify-content-around pt15 pb15 card-footer">
        <div class="block"><button class="w300 btn btn-main btn-lg" type="submit" data-form="#{{$id}}">
                <div class="fas fa-check mr10"></div>登録する
            </button></div>
    </div>
@endif
